Minimal functionality
    debugger::run should actually loop
        add quit command

    Expand waitpid wrapper to detect the state of the tracee

    use clap for command handling inside the io loop

Basic features
    Memory watchpoints
        read only
        write only
        read and write
    
    Function breakpoints

    File line breakpoints

    Step through code
        step over
        step into
        step out

    show values in Memory

    print variables

    call functions